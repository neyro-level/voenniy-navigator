<section
  class="vn-video-hero"
  data-review-id="podbor-hero"
  data-review-title="Подбор: первый экран"
  data-review-path="src/components/leadgen/sections/02-Hero.astro"
>
  <div class="vn-video-hero__grid" aria-hidden="true"></div>
  <div class="vn-container">
    <div class="vn-grid-12 vn-video-hero__layout">
      <div class="vn-video-hero__content" style="grid-column: 1 / 8">
        <span class="vn-label-mono vn-video-hero__label">Военная ипотека в Краснодаре</span>
        <h1 class="vn-video-hero__title">
          <span class="vn-video-hero__desktop-text">Подберем 5 вариантов квартир в Краснодаре под вашу военную ипотеку</span>
          <span class="vn-video-hero__mobile-text">Подберем 5 вариантов квартир в Краснодаре под вашу военную ипотеку</span>
        </h1>
        <p class="vn-video-hero__lead">
          <span class="vn-video-hero__desktop-text">Не нужно самому просматривать десятки ЖК и тысячи объявлений. Эксперт отберёт варианты под ваш бюджет, банк, район и задачу покупки.</span>
          <span class="vn-video-hero__mobile-text">Покажем варианты под вашу сумму, банк и район. Подскажем, где сейчас акции и скидки.</span>
        </p>

        <div class="vn-video-hero__service-note" aria-label="Условия услуги">
          <span>Бесплатно для покупателя</span>
          <span>
            <span class="vn-video-hero__desktop-text">Подскажем, где сейчас акции и скидки</span>
            <span class="vn-video-hero__mobile-text">Акции и скидки по ЖК</span>
          </span>
        </div>

        <div class="vn-video-hero__proof" aria-label="Что проверим перед подбором">
          <div class="vn-video-hero__proof-row">
            <span aria-hidden="true"></span>
            <p>
              <span class="vn-video-hero__desktop-text">5 вариантов квартир под вашу сумму и задачу</span>
              <span class="vn-video-hero__mobile-text">5 вариантов квартир под вашу сумму и ипотеку</span>
            </p>
          </div>
          <div class="vn-video-hero__proof-row">
            <span aria-hidden="true"></span>
            <p>
              <span class="vn-video-hero__desktop-text">Выбор банка и расчет стоимости покупки</span>
              <span class="vn-video-hero__mobile-text">Выбор банка и расчет полной стоимости</span>
            </p>
          </div>
          <div class="vn-video-hero__proof-row">
            <span aria-hidden="true"></span>
            <p>
              <span class="vn-video-hero__desktop-text">Выбор районов, определение сроков, анализ ликвидности</span>
              <span class="vn-video-hero__mobile-text">Районы, сроки сдачи и ликвидность объектов</span>
            </p>
          </div>
        </div>

        <div class="vn-video-hero__actions">
          <a
            href="/contacts/#request"
            class="vn-btn-primary vn-video-hero__cta"
            data-modal-open
            data-modal-title="Напишите ваше имя и телефон"
            data-modal-subtitle="Уточню ваш запрос и задачу, а затем подготовлю подборку квартир."
            data-modal-source="/podbor/hero"
          >
            <span class="vn-video-hero__desktop-text">Получить 5 вариантов квартир</span>
            <span class="vn-video-hero__mobile-text">Получить 5 вариантов</span>
          </a>
        </div>
        <p class="vn-video-hero__micro">
          <span class="vn-video-hero__desktop-text">Свяжемся в течение 15 минут, чтобы уточнить задачу.</span>
          <span class="vn-video-hero__mobile-text">Свяжемся в течение 15 минут.</span>
        </p>
      </div>

      <aside class="vn-video-hero__result" style="grid-column: 8 / 13" aria-label="Что вы получите после заявки">
        <div class="vn-video-hero__result-media">
          <img
            src="/images/krasnodar-newbuild-hero.png"
            alt="Современный жилой комплекс в Краснодаре"
            width="1600"
            height="900"
            loading="eager"
            class="vn-video-hero__image"
          />
          <span class="vn-video-hero__image-badge">ТОП-20 ЖК Краснодара</span>
        </div>

        <div class="vn-video-hero__result-body">
          <span class="vn-video-hero__result-kicker">Бесплатно для покупателя</span>
          <h2 class="vn-video-hero__result-title">Что вы получите после заявки</h2>
          <ul class="vn-video-hero__result-list">
            <li>
              <span class="vn-video-hero__desktop-text">Анализ застройщиков</span>
              <span class="vn-video-hero__mobile-text">Анализ застройщиков</span>
            </li>
            <li>
              <span class="vn-video-hero__desktop-text">Актуальные акции и скидки</span>
              <span class="vn-video-hero__mobile-text">Актуальные акции и скидки</span>
            </li>
            <li>
              <span class="vn-video-hero__desktop-text">Выбор банка и расчеты стоимости</span>
              <span class="vn-video-hero__mobile-text">Выбор банка и расчеты стоимости</span>
            </li>
            <li>
              <span class="vn-video-hero__desktop-text">Видеопрезентация жилых комплексов</span>
              <span class="vn-video-hero__mobile-text">Видеопрезентация жилых комплексов</span>
            </li>
          </ul>
        </div>
      </aside>
    </div>
  </div>
</section>

<style>
  .vn-video-hero {
    position: relative;
    overflow: hidden;
    padding: 86px 0 92px;
    background:
      linear-gradient(135deg, color-mix(in srgb, var(--color-bg-dark) 88%, var(--color-accent-primary)) 0%, var(--color-bg-dark) 62%),
      var(--color-bg-dark);
  }

  .vn-video-hero__grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(to right, var(--color-deco-grid-dark) 1px, transparent 1px),
      linear-gradient(to bottom, var(--color-deco-grid-dark) 1px, transparent 1px);
    background-size: 64px 64px;
    pointer-events: none;
  }

  .vn-video-hero__layout {
    position: relative;
    z-index: 1;
    align-items: center;
  }

  .vn-video-hero__mobile-text {
    display: none;
  }

  .vn-video-hero__content {
    min-width: 0;
    padding-right: 28px;
  }

  .vn-video-hero__label {
    color: var(--color-accent-cta);
  }

  .vn-video-hero__title {
    max-width: 720px;
    margin: 18px 0 0;
    color: var(--color-text-ondark);
    font-size: clamp(34px, 4vw, 48px);
    font-weight: 600;
    line-height: 1.1;
    letter-spacing: 0;
    text-wrap: balance;
  }

  .vn-video-hero__lead {
    max-width: 610px;
    margin: 22px 0 0;
    color: var(--color-text-ondark-mute);
    font-size: var(--fs-lead);
    line-height: 1.62;
    text-wrap: pretty;
  }

  .vn-video-hero__service-note {
    max-width: 620px;
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 22px;
  }

  .vn-video-hero__service-note > span {
    display: inline-flex;
    align-items: center;
    min-height: 32px;
    padding: 0 12px;
    border: 1px solid color-mix(in srgb, var(--color-accent-cta) 26%, transparent);
    border-radius: var(--radius-full);
    background: color-mix(in srgb, var(--color-accent-cta) 10%, transparent);
    color: var(--color-text-ondark);
    font-size: var(--fs-body-sm);
    font-weight: 650;
    line-height: 1;
    white-space: nowrap;
  }

  .vn-video-hero__proof {
    max-width: 620px;
    display: grid;
    gap: 0;
    margin-top: 24px;
    border-top: 1px solid color-mix(in srgb, var(--color-text-ondark) 10%, transparent);
  }

  .vn-video-hero__proof-row {
    display: grid;
    grid-template-columns: 24px minmax(0, 1fr);
    gap: 14px;
    align-items: start;
    padding: 15px 0;
    border-bottom: 1px solid color-mix(in srgb, var(--color-text-ondark) 10%, transparent);
  }

  .vn-video-hero__proof-row span {
    position: relative;
    width: 20px;
    height: 20px;
    margin-top: 1px;
    border: 1px solid color-mix(in srgb, var(--color-accent-cta) 34%, transparent);
    border-radius: var(--radius-full);
    background: color-mix(in srgb, var(--color-accent-cta) 10%, transparent);
    box-shadow: 0 0 0 4px color-mix(in srgb, var(--color-accent-cta) 7%, transparent);
  }

  .vn-video-hero__proof-row span::after {
    content: '';
    position: absolute;
    left: 7px;
    top: 4px;
    width: 5px;
    height: 9px;
    border-right: 2px solid var(--color-accent-cta);
    border-bottom: 2px solid var(--color-accent-cta);
    transform: rotate(42deg);
  }

  .vn-video-hero__proof-row p {
    margin: 0;
    color: var(--color-text-ondark);
    font-size: var(--fs-body);
    font-weight: 600;
    line-height: 1.4;
    text-wrap: pretty;
  }

  .vn-video-hero__actions {
    display: flex;
    align-items: center;
    margin-top: 32px;
  }

  .vn-video-hero__cta {
    flex: 0 0 auto;
  }

  .vn-video-hero__micro {
    max-width: 430px;
    margin: 12px 0 0;
    color: var(--color-text-ondark-mute);
    font-size: var(--fs-body-sm);
    line-height: 1.45;
  }

  .vn-video-hero__result {
    min-width: 0;
    position: relative;
    overflow: hidden;
    border: 1px solid color-mix(in srgb, var(--color-text-ondark) 12%, transparent);
    border-radius: 18px;
    background: var(--color-bg-dark-elev);
    box-shadow: var(--shadow-xl);
  }

  .vn-video-hero__result::before {
    content: '';
    position: absolute;
    inset: -8px;
    z-index: -1;
    border: 1px solid color-mix(in srgb, var(--color-accent-cta) 24%, transparent);
    border-radius: 22px;
  }

  .vn-video-hero__result-media {
    position: relative;
    min-height: 255px;
    overflow: hidden;
    background: var(--color-bg-dark);
  }

  .vn-video-hero__result-media::after {
    content: '';
    position: absolute;
    inset: 0;
    background:
      linear-gradient(to bottom, transparent 45%, color-mix(in srgb, var(--color-bg-dark) 42%, transparent) 100%),
      linear-gradient(135deg, color-mix(in srgb, var(--color-accent-primary) 18%, transparent), transparent 56%);
    pointer-events: none;
  }

  .vn-video-hero__image {
    width: 100%;
    height: 100%;
    min-height: 255px;
    display: block;
    object-fit: cover;
    object-position: center;
  }

  .vn-video-hero__image-badge {
    position: absolute;
    left: 22px;
    bottom: 20px;
    z-index: 1;
    padding: 8px 12px;
    border: 1px solid color-mix(in srgb, var(--color-text-ondark) 18%, transparent);
    border-radius: var(--radius-full);
    background: color-mix(in srgb, var(--color-bg-dark) 72%, transparent);
    color: var(--color-text-ondark);
    font-size: var(--fs-caption);
    font-weight: 600;
    line-height: 1.2;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    backdrop-filter: blur(10px);
  }

  .vn-video-hero__result-body {
    padding: 30px 34px 34px;
    background:
      linear-gradient(180deg, color-mix(in srgb, var(--color-bg-dark-elev) 92%, var(--color-accent-primary)) 0%, var(--color-bg-dark-elev) 100%);
    border-top: 1px solid color-mix(in srgb, var(--color-text-ondark) 10%, transparent);
  }

  .vn-video-hero__result-kicker {
    color: var(--color-accent-cta);
    font-size: var(--fs-caption);
    font-weight: 600;
    letter-spacing: 0.04em;
    line-height: 1.2;
    text-transform: uppercase;
  }

  .vn-video-hero__result-title {
    margin: 8px 0 0;
    color: var(--color-text-ondark);
    font-size: 26px;
    font-weight: 600;
    line-height: 1.15;
    text-wrap: balance;
  }

  .vn-video-hero__result-list {
    display: grid;
    gap: 0;
    margin: 22px 0 0;
    padding: 0;
    list-style: none;
  }

  .vn-video-hero__result-list li {
    position: relative;
    padding: 14px 0 14px 34px;
    border-top: 1px solid color-mix(in srgb, var(--color-text-ondark) 10%, transparent);
    color: var(--color-text-ondark);
    font-size: var(--fs-body);
    line-height: 1.44;
    text-wrap: pretty;
  }

  .vn-video-hero__result-list li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 15px;
    width: 20px;
    height: 20px;
    border-radius: var(--radius-full);
    background: var(--color-accent-cta-soft);
    border: 1px solid color-mix(in srgb, var(--color-accent-cta) 34%, transparent);
    box-shadow: 0 0 0 4px color-mix(in srgb, var(--color-accent-cta) 8%, transparent);
  }

  .vn-video-hero__result-list li::after {
    content: '';
    position: absolute;
    left: 7px;
    top: 19px;
    width: 6px;
    height: 10px;
    border-right: 2px solid var(--color-accent-cta);
    border-bottom: 2px solid var(--color-accent-cta);
    transform: rotate(42deg);
  }

  @media (max-width: 1180px) {
    .vn-video-hero {
      padding: 66px 0 72px;
    }

    .vn-video-hero__content {
      padding-right: 0;
    }

    .vn-video-hero__result-body {
      padding: 24px;
    }
  }

  @media (max-width: 1023px) {
    .vn-video-hero {
      padding: 54px 0 60px;
    }

    .vn-video-hero__content,
    .vn-video-hero__result {
      grid-column: 1 / -1 !important;
    }

    .vn-video-hero__content {
      max-width: 760px;
    }

    .vn-video-hero__result {
      width: min(100%, 620px);
      margin-top: 36px;
    }
  }

  @media (max-width: 640px) {
    .vn-video-hero {
      padding: 32px 0 42px;
    }

    .vn-video-hero__title {
      margin-top: 14px;
      font-size: 29px;
      line-height: 1.1;
    }

    .vn-video-hero__desktop-text {
      display: none;
    }

    .vn-video-hero__mobile-text {
      display: inline;
    }

    .vn-video-hero__lead {
      margin-top: 16px;
      font-size: 16px;
      line-height: 1.55;
    }

    .vn-video-hero__service-note {
      gap: 8px;
      margin-top: 18px;
    }

    .vn-video-hero__service-note > span {
      min-height: 30px;
      font-size: 12px;
    }

    .vn-video-hero__service-note > span {
      max-width: 100%;
      white-space: normal;
      line-height: 1.2;
    }

    .vn-video-hero__bullets {
      gap: 9px;
      margin-top: 22px;
    }

    .vn-video-hero__bullets li {
      padding-left: 22px;
      font-size: 14px;
      line-height: 1.45;
    }

    .vn-video-hero__actions {
      display: grid;
      gap: 12px;
      margin-top: 24px;
    }

    .vn-video-hero__cta {
      width: 100%;
      justify-content: center;
      min-height: 50px;
    }

    .vn-video-hero__micro {
      max-width: none;
      text-align: center;
    }

    .vn-video-hero__result {
      width: 100%;
      margin-top: 28px;
      border-radius: var(--radius-lg);
    }

    .vn-video-hero__result::before {
      display: none;
    }

    .vn-video-hero__proof {
      margin-top: 24px;
    }

    .vn-video-hero__proof-row {
      padding: 12px 0;
    }

    .vn-video-hero__actions {
      margin-top: 24px;
    }

    .vn-video-hero__result-media,
    .vn-video-hero__image {
      min-height: 170px;
    }

    .vn-video-hero__image-badge {
      left: 16px;
      bottom: 14px;
      max-width: calc(100% - 32px);
    }

    .vn-video-hero__result-body {
      padding: 22px 20px 24px;
    }

    .vn-video-hero__result-title {
      font-size: 22px;
    }

    .vn-video-hero__result-list {
      gap: 12px;
      margin-top: 18px;
    }

    .vn-video-hero__result-list li {
      padding-left: 30px;
      font-size: 14px;
    }
  }
</style>
