<!-- ══════════════════════════════════════════════
     БЛОК 02 — ПОДХОД К ВЫБОРУ
════════════════════════════════════════════════ -->
<section id="section-short-answer" class="vn-home-answer" aria-labelledby="vn-home-answer-title">
  <div class="vn-container">
    <div class="vn-home-answer__inner vn-grid-12">

      <div class="vn-home-answer__label" style="grid-column: 1 / 4">
        <span class="vn-label-mono">Коротко о подходе</span>
      </div>

      <div class="vn-home-answer__main" style="grid-column: 4 / 9">
        <h2 id="vn-home-answer-title">Сначала разбираем задачу, потом смотрим новостройки</h2>
        <p>
          Не стоит начинать покупку с каталога на агрегаторах или случайного жилого комплекса.
          Одна и та же новостройка может быть хорошим вариантом для жизни, но слабым вложением,
          и наоборот.
        </p>
      </div>

      <p class="vn-home-answer__detail" style="grid-column: 9 / 13">
        На первичном разборе я уточню цель покупки, бюджет, сроки, ограничения по военной ипотеке
        и удобный формат сделки. После этого станет понятно, какие новостройки действительно стоит
        рассматривать.
      </p>

    </div>
  </div>
</section>

<style>
/* ─── Block 02: Short answer ─── */
.vn-home-answer {
  background: var(--color-bg-surface);
  padding-block: 52px;
  border-top: 1px solid var(--color-border);
  border-bottom: 1px solid var(--color-border);
}

.vn-home-answer__inner {
  align-items: start;
}

.vn-home-answer__label {
  padding-top: 4px;
}

.vn-home-answer__main {
  max-width: 620px;
}

.vn-home-answer__main h2 {
  margin: 0;
  font-size: var(--fs-h2);
  font-weight: 600;
  line-height: 1.18;
  letter-spacing: 0;
  color: var(--color-text-primary);
  text-wrap: balance;
}

.vn-home-answer__main p {
  margin: 18px 0 0;
  font-size: var(--fs-body);
  line-height: 1.7;
  color: var(--color-text-secondary);
  text-wrap: pretty;
}

.vn-home-answer__detail {
  margin: 0;
  font-size: var(--fs-body-sm);
  line-height: 1.75;
  color: var(--color-text-tertiary);
  padding-left: 24px;
  border-left: 1px solid var(--color-border);
  text-wrap: pretty;
}

@media (max-width: 1023px) {
  .vn-home-answer__label,
  .vn-home-answer__main,
  .vn-home-answer__detail {
    grid-column: 1 / -1 !important;
  }

  .vn-home-answer__main {
    margin-top: 22px;
    max-width: none;
  }

  .vn-home-answer__detail {
    padding-left: 0;
    border-left: none;
    border-top: 1px solid var(--color-border);
    padding-top: 20px;
    margin-top: 24px;
  }
}

@media (max-width: 640px) {
  .vn-home-answer { padding-block: 36px; }
  .vn-home-answer__main h2 { font-size: var(--fs-h3); }
}
</style>
