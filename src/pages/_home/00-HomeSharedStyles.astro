<style is:global>
/* ─── Helpers ─── */
.vn-rel  { position: relative; z-index: 1; }
.vn-pos-tr { right: -40px; top: -40px; }
.vn-pos-bl { left: -40px; bottom: -40px; }

.vn-home-h2 {
  font-size: clamp(28px, 3.5vw, 36px);
  font-weight: 600;
  line-height: 1.15;
  letter-spacing: -0.015em;
  color: var(--color-text-primary);
  margin: 0;
}

.vn-home-sub {
  font-size: 19px;
  font-weight: 400;
  line-height: 1.5;
  color: var(--color-text-secondary);
  margin: 0;
}

.vn-home-textlink {
  font-size: 15px;
  font-weight: 500;
  color: var(--color-text-primary);
  text-decoration: none;
  border-bottom: 1px solid transparent;
  transition: border-color 0.15s ease;
}
.vn-home-textlink:hover { border-bottom-color: var(--color-text-primary); }

.vn-accent-line {
  width: 2px;
  height: 24px;
  background-color: var(--color-accent-cta);
  border-radius: 2px;
  display: block;
  flex-shrink: 0;
}
</style>
