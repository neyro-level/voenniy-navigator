<!-- ══════════════════════════════════════════════
     БЛОК 01 — HERO
════════════════════════════════════════════════ -->
<section id="section-hero" class="vn-home-hero">
  <div class="vn-home-hero__grid-bg" aria-hidden="true"></div>
  <div class="vn-home-hero__radial" aria-hidden="true"></div>

  <div class="vn-container">
    <div class="vn-home-hero__inner vn-grid-12">

      <!-- Left: 6 колонок -->
      <div class="vn-home-hero__left">
        <div class="vn-home-hero__eyebrow">Военная ипотека · новостройки</div>

        <h1 class="vn-home-hero__h1">
          Помогу выбрать новостройку по военной ипотеке
          <span class="vn-home-hero__h1-accent"> под вашу задачу</span>
        </h1>

        <p class="vn-home-hero__sub">
          Сначала разберём, для чего вы покупаете квартиру: для жизни или как вложение.
          После этого определим, какие районы, жилые комплексы, сроки сдачи и формат сделки подходят
          под вашу ситуацию.
        </p>

        <div class="vn-home-hero__cta-row">
          <a
            href="/contacts/#request"
            data-modal-open
            data-modal-title="Получить первичный разбор"
            class="vn-btn-primary vn-home-hero__btn-primary"
          >
            Получить первичный разбор
          </a>
        </div>

        <p class="vn-home-hero__cta-note">Я уточню вашу задачу и подскажу, с чего начать.</p>

        <div class="vn-home-hero__proof vn-grid-12" role="list" aria-label="Короткие доказательства">
          <div class="vn-home-hero__proof-card" style="grid-column: 1 / 7" role="listitem">
            <span class="vn-home-hero__proof-label">Опыт</span>
            <span class="vn-home-hero__proof-value">В недвижимости с&nbsp;2016&nbsp;года</span>
          </div>
          <div class="vn-home-hero__proof-card" style="grid-column: 7 / 13" role="listitem">
            <span class="vn-home-hero__proof-label">Военная ипотека</span>
            <span class="vn-home-hero__proof-value">65&nbsp;сделок по военной ипотеке</span>
          </div>
          <div class="vn-home-hero__proof-card" style="grid-column: 1 / 7" role="listitem">
            <span class="vn-home-hero__proof-label">Практика</span>
            <span class="vn-home-hero__proof-value">180&nbsp;проведённых сделок</span>
          </div>
          <div class="vn-home-hero__proof-card" style="grid-column: 7 / 13" role="listitem">
            <span class="vn-home-hero__proof-label">Формат</span>
            <span class="vn-home-hero__proof-value">Можно начать дистанционно</span>
          </div>
        </div>
      </div>

      <!-- Right: 6 колонок -->
      <div class="vn-home-hero__right">
        <div class="vn-home-hero__photo-outer">
          <div class="vn-home-hero__portrait-frame">
            <img
              src="/images/mikhail-hero.png"
              alt="Михаил Хряпин — эксперт по выбору новостроек по военной ипотеке"
              loading="eager"
              decoding="async"
              class="vn-home-hero__portrait"
            />
            <div class="vn-home-hero__name-card">
              <p class="vn-home-hero__name-card-title">Михаил Хряпин</p>
              <p class="vn-home-hero__name-card-sub">Помогаю военнослужащим выбрать новостройку по военной ипотеке</p>
            </div>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>

<style>
/* ─── Block 01: Hero ─── */
.vn-home-hero {
  position: relative;
  overflow: hidden;
  background-color: var(--color-bg-primary);
  padding-top: 160px;
  padding-bottom: 96px;
  min-height: 100vh;
  display: flex;
  align-items: center;
}

.vn-home-hero__grid-bg {
  position: absolute;
  inset: 0;
  background-image:
    linear-gradient(to right, var(--color-deco-grid) 1px, transparent 1px),
    linear-gradient(to bottom, var(--color-deco-grid) 1px, transparent 1px);
  background-size: 64px 64px;
  pointer-events: none;
  z-index: 0;
}

.vn-home-hero__radial {
  position: absolute;
  top: -200px;
  left: -200px;
  width: 800px;
  height: 800px;
  background: radial-gradient(circle, color-mix(in srgb, var(--color-bg-surface) 60%, transparent) 0%, transparent 70%);
  pointer-events: none;
  z-index: 0;
}

.vn-home-hero__inner {
  position: relative;
  z-index: 1;
  align-items: start;
  width: 100%;
}

.vn-home-hero__left { min-width: 0; }

.vn-home-hero__eyebrow {
  display: flex;
  align-items: center;
  gap: 12px;
  font-size: var(--fs-label);
  font-weight: 500;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  color: var(--color-text-secondary);
  margin-bottom: 28px;
}
.vn-home-hero__eyebrow::before {
  content: "";
  width: 32px;
  height: 2px;
  background: var(--color-accent-cta);
  flex-shrink: 0;
}

.vn-home-hero__h1 {
  margin: 0 0 24px;
  font-size: var(--fs-display);
  line-height: 1.05;
  font-weight: 600;
  letter-spacing: -0.03em;
  color: var(--color-text-primary);
  text-wrap: balance;
}

.vn-home-hero__h1-accent { color: var(--color-accent-primary); }

.vn-home-hero__sub {
  margin: 0 0 36px;
  font-size: var(--fs-lead);
  line-height: 1.5;
  color: var(--color-text-secondary);
  text-wrap: balance;
}

.vn-home-hero__cta-row {
  display: flex;
  gap: 12px;
  margin-bottom: 12px;
}

.vn-home-hero__btn-primary {
  height: 54px;
  padding: 0 40px;
  font-size: 16px;
}

.vn-home-hero__cta-note {
  margin: 0;
  font-size: var(--fs-body-sm);
  line-height: 1.55;
  color: var(--color-text-tertiary);
  text-wrap: pretty;
}

.vn-home-hero__proof {
  margin-top: 30px;
  gap: 10px;
}

.vn-home-hero__proof-card {
  min-width: 0;
  padding: 18px 18px 17px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  background: color-mix(in srgb, var(--color-bg-surface) 58%, transparent);
}

.vn-home-hero__proof-label {
  display: block;
  font-size: var(--fs-label);
  font-weight: 500;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  color: var(--color-text-tertiary);
  margin-bottom: 6px;
}

.vn-home-hero__proof-value {
  display: block;
  font-size: var(--fs-body-sm);
  line-height: 1.4;
  font-weight: 500;
  color: var(--color-text-primary);
}

/* Photo column */
.vn-home-hero__left  { grid-column: 1 / 8; }

.vn-home-hero__right {
  grid-column: 8 / 13;
  position: relative;
  min-width: 0;
  padding-top: 40px;
}

.vn-home-hero__photo-outer {
  position: relative;
  max-width: 420px;
}

.vn-home-hero__portrait-frame {
  position: relative;
  width: 100%;
  aspect-ratio: 3 / 4;
  border-radius: 12px;
  overflow: hidden;
  box-shadow:
    14px 14px 0 color-mix(in srgb, var(--color-accent-primary) 18%, transparent),
    0 24px 64px color-mix(in srgb, var(--color-bg-dark) 14%, transparent);
}

.vn-home-hero__portrait {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center top;
  display: block;
}

/* Glassmorphism name card */
.vn-home-hero__name-card {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: color-mix(in srgb, var(--color-bg-dark) 65%, transparent);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  padding: 16px 20px;
}

.vn-home-hero__name-card-title {
  margin: 0 0 4px;
  font-size: 16px;
  font-weight: 600;
  color: var(--color-bg-surface);
}

.vn-home-hero__name-card-sub {
  margin: 0;
  font-size: var(--fs-body-sm);
  line-height: 1.4;
  color: var(--color-text-ondark-mute);
}

@media (max-width: 1023px) {
  .vn-home-hero__left  { grid-column: 1 / -1; }
  .vn-home-hero__right { grid-column: 1 / -1; padding-top: 0; }
  .vn-home-hero__photo-outer { max-width: 400px; margin: 0 auto; }
  .vn-home-hero__right { margin-top: 48px; }

  .vn-home-hero__proof.vn-grid-12 > .vn-home-hero__proof-card:nth-child(odd) {
    grid-column: 1 / 7 !important;
  }

  .vn-home-hero__proof.vn-grid-12 > .vn-home-hero__proof-card:nth-child(even) {
    grid-column: 7 / 13 !important;
  }
}

@media (max-width: 640px) {
  .vn-home-hero { padding-top: 100px; padding-bottom: 64px; }
  .vn-home-hero__grid-bg { background-size: 48px 48px; }
  .vn-home-hero__h1 { line-height: 1.1; letter-spacing: -0.02em; }
  .vn-home-hero__cta-row { margin-bottom: 12px; }
  .vn-home-hero__btn-primary { width: 100%; height: 52px; justify-content: center; }
  .vn-home-hero__proof { margin-top: 24px; gap: 8px; }
  .vn-home-hero__proof-card { padding: 14px 12px; }
  .vn-home-hero__right { margin-top: 40px; }
  .vn-home-hero__portrait-frame {
    aspect-ratio: 4 / 5;
    border-radius: 8px;
    box-shadow:
      8px 8px 0 color-mix(in srgb, var(--color-accent-primary) 15%, transparent),
      0 16px 40px color-mix(in srgb, var(--color-bg-dark) 12%, transparent);
  }
  .vn-home-hero__name-card { padding: 12px 16px; }
}
</style>
