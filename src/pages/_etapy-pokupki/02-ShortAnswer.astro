<section id="section-etap-answer" class="vn-etap-answer" aria-labelledby="vn-etap-answer-title">
  <div class="vn-container">
    <div class="vn-grid-12 vn-etap-answer__grid">
      <blockquote class="vn-etap-answer__quote" id="vn-etap-answer-title" style="grid-column: 1 / 9">
        Покупка новостройки по военной ипотеке в Краснодаре проходит не одним действием,
        а маршрутом из нескольких этапов.
      </blockquote>

      <p class="vn-etap-answer__detail" style="grid-column: 9 / 13">
        Сначала важно понять цель, сценарий, банк и документы. Затем уже выбирать объект,
        проходить согласования и регистрацию без нарушения последовательности.
      </p>
    </div>
  </div>
</section>

<style>
  .vn-etap-answer {
    padding-block: 52px;
    background: var(--color-bg-surface);
    border-top: 1px solid var(--color-border);
    border-bottom: 1px solid var(--color-border);
  }

  .vn-etap-answer__grid {
    align-items: center;
  }

  .vn-etap-answer__quote {
    margin: 0;
    font-size: clamp(20px, 1.9vw, 26px);
    font-weight: 300;
    line-height: 1.5;
    letter-spacing: -0.02em;
    color: var(--color-text-primary);
    text-wrap: balance;
  }

  .vn-etap-answer__detail {
    margin: 0;
    padding-left: 24px;
    border-left: 1px solid var(--color-border);
    font-size: 13px;
    line-height: 1.75;
    color: var(--color-text-tertiary);
    text-wrap: pretty;
  }

  @media (max-width: 1023px) {
    .vn-etap-answer__quote,
    .vn-etap-answer__detail {
      grid-column: 1 / -1 !important;
    }

    .vn-etap-answer__detail {
      margin-top: 4px;
      padding-top: 20px;
      padding-left: 0;
      border-top: 1px solid var(--color-border);
      border-left: none;
    }
  }

  @media (max-width: 640px) {
    .vn-etap-answer {
      padding-block: 36px;
    }

    .vn-etap-answer__quote {
      font-size: 18px;
    }
  }
</style>
