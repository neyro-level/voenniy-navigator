<!-- ══════════════════════════════════════════════
     БЛОК 06 — МЕТОД (тёмная секция)
════════════════════════════════════════════════ -->
<section id="section-krym-method" class="vn-section vn-section--dark">
  <div class="vn-deco-grid vn-deco-grid--dark" aria-hidden="true"></div>
  <div class="vn-container">
    <div class="vn-grid-12">
      <!-- Left: text -->
      <div class="vn-krym-method__left">
        <span class="vn-label-mono">Метод / 06</span>
        <h2 class="vn-krym-h2" style="margin-top: 16px; color: var(--color-text-ondark); max-width: 520px;">
          Почему важен разбор задачи до начала подбора
        </h2>
        <p class="vn-krym-sub" style="margin-top: 12px; color: var(--color-text-ondark-mute); max-width: 520px;">
          Выбрать квартиру в Крыму без понимания цели — значит тратить время и
          рисковать выбором не того объекта, района или формата. Разбор задачи
          заранее определяет, какой город, район и сценарий подходят именно вам.
        </p>
        <a
          href="/contacts/#request"
          data-modal-open
          data-modal-title="Разобрать мою задачу"
          class="vn-btn-primary vn-krym-method__cta"
        >
          Разобрать мою задачу
        </a>
      </div>

      <!-- Right: big number -->
      <div class="vn-krym-method__right">
        <div class="vn-krym-method__stat">
          <span class="vn-krym-method__number">65+</span>
          <span class="vn-krym-method__label">сделок и разборов задач по Крыму и Краснодару с&nbsp;2016&nbsp;года</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .vn-krym-method__left {
    grid-column: 1 / 7;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }

  .vn-krym-method__right {
    grid-column: 7 / 13;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  .vn-krym-method__stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .vn-krym-method__number {
    font-family: 'Fira Code', monospace;
    font-size: clamp(56px, 8vw, 96px);
    font-weight: 400;
    color: var(--color-text-ondark);
    letter-spacing: -0.02em;
    line-height: 1;
  }

  .vn-krym-method__label {
    font-size: 15px;
    font-weight: 400;
    color: var(--color-text-ondark-mute);
    margin-top: 12px;
    line-height: 1.45;
    max-width: 320px;
  }

  .vn-krym-method__cta {
    margin-top: 32px;
    display: inline-flex;
  }

  @media (max-width: 1023px) {
    .vn-krym-method__left,
    .vn-krym-method__right { grid-column: 1 / -1; }

    .vn-krym-method__right {
      margin-top: 48px;
      align-items: flex-start;
    }
    .vn-krym-method__stat { align-items: flex-start; text-align: left; }
  }
</style>
