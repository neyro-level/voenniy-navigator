<style is:global>
/* ─── Shared utilities ─── */
.vn-krym-h2 {
  font-size: var(--fs-h1);
  font-weight: 600;
  line-height: 1.1;
  letter-spacing: -0.02em;
  color: var(--color-text-primary);
  margin: 0;
  text-wrap: balance;
}

.vn-krym-sub {
  font-size: var(--fs-lead);
  line-height: 1.55;
  color: var(--color-text-secondary);
  margin: 0;
}

.vn-krym-textlink {
  font-size: 15px;
  font-weight: 500;
  color: var(--color-text-primary);
  text-decoration: none;
  border-bottom: 1px solid transparent;
  transition: border-color 0.15s ease;
}
.vn-krym-textlink:hover { border-bottom-color: var(--color-text-primary); }

/* ─── Decorative elements ─── */
.vn-krym-accent-line {
  width: 2px;
  height: 24px;
  background-color: var(--color-accent-cta);
  border-radius: 2px;
  display: block;
  flex-shrink: 0;
}

/* ─── Dark section deco grid ─── */
.vn-krym-deco-grid {
  position: absolute;
  inset: 0;
  background-image:
    linear-gradient(to right, var(--color-deco-grid-dark) 1px, transparent 1px),
    linear-gradient(to bottom, var(--color-deco-grid-dark) 1px, transparent 1px);
  background-size: 64px 64px;
  pointer-events: none;
  z-index: 0;
}

/* ─── Responsive helpers ─── */
@media (max-width: 1023px) {
  .vn-krym-hide-mobile { display: none !important; }
}
</style>
