<!-- ══════════════════════════════════════════════
     БЛОК 02 — КОРОТКИЙ ОТВЕТ
════════════════════════════════════════════════ -->
<section id="section-krym-answer" class="vn-section" style="background-color: var(--color-bg-surface);">
  <div class="vn-container">
    <div class="vn-grid-12">
      <div class="vn-krym-answer__inner">
        <span class="vn-label-mono">Кратко о странице</span>
        <p class="vn-krym-answer__text">
          Купить квартиру или новостройку в Крыму по военной ипотеке можно, но
          начинать лучше не с просмотра всех объявлений, а с разбора цели
          покупки. Для жизни, переезда позже, сдачи и дистанционной сделки
          подходят разные города, районы, сроки сдачи и форматы объектов.
          В Крыму важно заранее проверить локацию, документы, банк, застройщика,
          возможность дистанционного маршрута и дальнейшее использование квартиры.
        </p>

        <div class="vn-krym-answer__pills">
          <span class="vn-krym-answer__pill">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
            Не все объекты подходят под один сценарий
          </span>
          <span class="vn-krym-answer__pill">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
            Крым требует проверки города, района и документов
          </span>
          <span class="vn-krym-answer__pill">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
            Разбор нужен до подбора вариантов
          </span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .vn-krym-answer__inner {
    grid-column: 3 / 11;
    padding: 64px 0;
  }

  .vn-krym-answer__text {
    margin: 16px 0 32px;
    font-size: clamp(16px, 1.2vw, 19px);
    line-height: 1.6;
    color: var(--color-text-secondary);
    padding-left: 24px;
    border-left: 3px solid color-mix(in srgb, var(--color-accent-primary) 15%, transparent);
    text-wrap: pretty;
  }

  .vn-krym-answer__pills {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
  }

  .vn-krym-answer__pill {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 16px;
    background-color: var(--color-accent-soft);
    border: 1px solid color-mix(in srgb, var(--color-accent-primary) 20%, transparent);
    border-radius: 999px;
    font-size: 14px;
    font-weight: 500;
    color: var(--color-accent-primary);
  }
  .vn-krym-answer__pill svg { color: var(--color-accent-cta); flex-shrink: 0; }

  @media (max-width: 1023px) {
    .vn-krym-answer__inner { grid-column: 1 / -1; }
  }

  @media (max-width: 640px) {
    .vn-krym-answer__inner { padding: 48px 0; }
    .vn-krym-answer__text { font-size: 16px; }
  }
</style>
